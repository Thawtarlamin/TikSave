{
  "name": "authorized-tiktok-media-api",
  "version": "1.0.0",
  "private": true,
  "description": "Authorized TikTok media API using Node.js and Express",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/downloader.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "supertest": "^7.1.0"
  }
}
